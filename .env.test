# Mode-scoped env consumed by `vite build --mode test` (used by the Playwright
# webServer). Loaded with higher precedence than the catch-all `.env.local`,
# so the E2E suite reliably runs against in-app mock data regardless of
# whatever a developer has set locally.
VITE_USE_MOCK=true
